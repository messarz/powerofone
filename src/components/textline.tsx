import{component$}from"@builder.io/qwik";export const HelloWorld=component$(()=>{return(<h1>Power Of One</h1>);});export const Future=component$(()=>{return(<h1>Technology</h1>);});export const Past=component$(()=>{return(<h1>One</h1>);});

/*
import { component$ } from "@builder.io/qwik";

export const HelloWorld = component$(() => {

    return (
        <h1>
            Power Of One
        </h1>
    );
});


    export const Future = component$(() => {

        return (
            <h1>
                Technology
            </h1>
        );
});

export const Past = component$(() => {

    return (
        <h1>
            One
        </h1>
    );
});

*/